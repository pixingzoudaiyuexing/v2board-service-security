## 1.1.4

- add desktop compatibility endpoints for CloudGap/FlClash style clients
- support `/api/v1/config`, `/api/v1/plan`, `/api/v1/node`
- add safe compatibility handling for `/api/v1/client/app/getVersion`
- map desktop compatibility requests to existing v2board data endpoints
- keep Koa server and Cloudflare Worker behavior aligned
